<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Holidays</title>
    <script src="/index.js"></script>
    <link rel="stylesheet" href="styles/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Custom CSS for centering the form */
        body, html {
            height: 100%;
        }
        body {
            display: flex;
            background-color: #f0f0f0; 
        }
        .form-signin {
            max-width: 80000px;
            padding: 10px;
            justify-content: center;
            margin-top: 100px;
            margin-left: 200px;
            margin-bottom: 10px; 
        }
        .noteandformat {
            margin-top: 400px; 
            margin-right: 333px;
            margin-left: 33px;
            /* Adjust spacing as needed */
            text-align: center; /* Center align content */
        }
        .noteandformat p {
            font-weight: bold; /* Use 'font-weight: bold;' instead of 'font: bolder;' */
        }
        .noteandformat a {
            text-decoration: none;
        }
    </style>
</head>
<body>
    <main class="form-signin">
        <form action="/holidays" method="post">
            <h1 class="h3 mb-3 fw-normal text-center">Enter the Country name and year</h1>
            <div class="form-floating mb-3">
                <input type="text" class="form-control" id="country" name="country" placeholder="Enter the country name in iso-3166 format">
                <label for="country">Country Name</label>
            </div>
            <div class="form-floating mb-3">
                <input type="text" class="form-control" id="year" name="year" placeholder="Year">
                <label for="year">Year</label>
            </div>
            <button class="btn btn-primary w-100 py-2" type="submit">Search Holidays</button>
        </form>
    </main>
    <div class="noteandformat">
        <p id="note">
            Note: You need to enter the Country name in iso-3166 format like in for (india).<br>
            Click <a href="https://countryconverter.com/">Here</a> to Get The ISO Format.
        </p>
    </div>
</body>
</html>
